PORT=5000
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/portfolio?retryWrites=true&w=majority
# Optional: origin used by the client during development (Vite default)
CLIENT_ORIGIN=http://localhost:5173

# NODE_ENV=production to enable serving the built client from ../client/dist
# For production, build the client (from the client folder) using `npm run build` and set NODE_ENV=production
# PORT can be changed if needed
# Example production env:
# PORT=5000
# MONGODB_URI=mongodb+srv://user:pass@cluster0.mongodb.net/portfolio
# NODE_ENV=production